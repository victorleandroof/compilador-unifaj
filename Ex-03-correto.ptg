programa
   decimal : x , y ;
   decimal:z,media;
   imprima ( "Digite 3 nros:" );
   leia ( x ) ;
   leia (y) ;
   leia(z);
   media:=(x+y+z)/3;     // Calcula e
   imprima ("Media = "); // mostra o
   imprima(media);       // resultado
fim_programa
